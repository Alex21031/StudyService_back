Frontend snapshot for the StudyService web client.

This folder mirrors the current Flutter web app source from the workspace root
so it can be versioned inside the backend repository when the original frontend
Git metadata is unavailable.
